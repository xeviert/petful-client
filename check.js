addToList = () => {
    // const {people} = this.state;

    names = ['Xevier Tee', 'Louis Cid', 'Tara Pommy', 'Izzy Stankovic', 'Stephanie Orea', 'Gavin Rodriguez', 'Peyton Flores', 'Sarah MF Jackson']

    randomName = names[Math.floor(Math.random()*names.length)];

    console.log(randomName)

    // if (people.length < 5) {
        
    // }
}

addToList();